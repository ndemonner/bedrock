{application, bedrock,
 [
  {description, "Rock-solid remote data storage for apps."},
  {vsn, "0.0.1"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib
                 ]},
  {mod, { bedrock_app, []}},
  {env, [
      {pools, [
          {router_pool, [
              {size, 10},
              {max_overflow, 20}
          ]}
      ]}
  ]}
 ]}.
